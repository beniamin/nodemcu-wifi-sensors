#ifndef Config_H
#define Config_H

namespace Config {
        const char *WIFI_SSID = "$WIFI_SSID";
        const char *WIFI_PASSWORD = "$WIFI_PASSWORD";
        const char *IOT_SERVER = "$IOT_SERVER";
        const char *EOL_VAR_TEST = "EOL";
}

#endif